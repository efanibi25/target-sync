import { printLine } from './modules/print';

console.debug('Content script works!');
console.debug('Must reload extension for modifications to take effect.');

printLine("Using the 'printLine' function from the Print Module");
